# ProjektDatateknik

Allmän info:
- Credential helper kan man använda för att cache:a användarnamn/lösenord så att man slipper skriva in det varje gång man vill köra git pull/push

För att klona projektet:
1. Gör en mapp som (förslagsvis) heter Git
2. -> git init (initierar mappen som en repository)
3. -> git clone https://github.com/flottbulle/ProjektDatateknik.git

För att hämta:
cd till repositoryn
-> git pull

För att skicka:
cd till repositoryn
-> git add .   (lägger till allt i repositoryn)
-> git commit -m "Meddelande för din ändring"
-> git push     (pushar ändringarna till servern)
